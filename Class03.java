// (a)
this(10)

// (b)
第21行是呼叫沒有引數的，第22行則是呼叫有引數的。

// (c)
Caaa 類別不能宣告成 public，因為一個檔案裡只能有一個 public 的類別，而且與程式儲存的文字檔名相同。如果要將 Caaa 類別宣告成 public，則必須將Caaa 類別另存成一個檔案，同時該檔案的名稱要為 Caaa.java。

// (d)
可在其他類別呼叫 value 所以並沒有影響。
